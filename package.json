{
  "name": "@modiapp/client",
  "version": "0.6.6",
  "description": "Serves as a Typescript api to @modi/server",
  "main": "dist/GameRoomClient.js",
  "files": [
    "dist/",
    "index.d.ts"
  ],
  "types": "index.d.ts",
  "repository": "git://github.com/modiapp/client.git",
  "author": {
    "name": "Isaac J Benzaken",
    "email": "ikey.benz@gmail.com"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "license": "UNLICENSED",
  "private": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prebuild": "rm -rf ./dist",
    "deploy": "yarn build && source .env && npm publish"
  },
  "dependencies": {
    "socket.io-client": "^2.3.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@types/socket.io-client": "^1.4.34",
    "typescript": "^4.0.5"
  }
}